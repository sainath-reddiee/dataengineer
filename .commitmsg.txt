linking: broaden internal discovery across articles, 404, and search (Phase 2)

Internal linking & PSEO cross-discovery pass to improve crawl depth,
dwell time, and SERP share for article traffic.

- Add src/data/articleToolMap.js: category/tag -> curated cheatsheets,
  comparisons, and glossary terms (with alias normalization).
- ArticlePage: render <RelatedTools/> block above RelatedPosts, move
  RelatedPosts above GiscusComments so discovery happens before the
  thread, pass post category to related-posts hook.
- useRelatedPosts: fall back to latest posts from the same category
  when the WP /related endpoint returns nothing, keeping old signature
  backward compatible.
- NotFoundPage: add search form, 4 popular landings, and 9 category
  chips so 404 traffic is rescued to high-intent destinations.
- Header: getCtaUrl() now derives real slugs from group items -
  single-item groups go direct to the category page, multi-item groups
  produce /articles?categories=a,b,c instead of a bare /articles.
- SearchModal: merge local searchIndex.json (glossary + comparisons)
  with WP article results, de-duped by URL and tagged with type badges
  (Article / Glossary / Comparison) plus a short excerpt line.

Build verified: 2044 modules transformed, built in 19.34s, 0 errors.
