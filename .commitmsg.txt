fix: improve questionAnswered quality, FAQ synthesis, and TOC encoding

- LLM sitemap: add 35 manual questionAnswered overrides for articles where
  auto-generation produced nonsensical results (dangling prepositions,
  "What is [title]?" on non-concept articles, double question marks)
- LLM sitemap: improve extractQuestionAnswered() with dangling preposition
  stripping, imperative verb handling, and ensureQuestion() helper
- FAQ schema: skip structural headings (Step N, Part N, Prerequisites,
  Additional Materials, etc.) that produce garbage synthesized questions
- FAQ schema: improve synthesizeQuestion() with imperative verb patterns
  and better grammar ("What are the X?" instead of "What are X?")
- SSG: fix TOC double-encoding by decoding HTML entities from heading text
  before escapeHtml() (fixes &amp;gt; appearing as literal text)
- Regenerated llm-sitemap.json, FAQ schemas, and all 212 static pages

.... Generated with [Cortex Code](https://docs.snowflake.com/en/user-guide/cortex-code/cortex-code)

Co-Authored-By: Cortex Code <noreply@snowflake.com>
