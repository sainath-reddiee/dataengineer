# Build cache for incremental builds
.build-cache.json
.pseo-cache.json
.articles-cache.json

# Sitemap cache files (optional - can commit them too)
.sitemap-cache.json
.notification-cache.json

# Build output
dist/
dist-ssr/
*.local

# Lint artifacts
lint_results.json

# Build / validator logs (local-only)
build.log
scripts/_validate.cjs
scripts/_audit.cjs
scripts/_v.cjs

# Node
node_modules/

# Environment files
.env
.env.local
.env.production

# Don't ignore example
!.env.example

